// 最小構成。secret-verify (frontend-ci) が wrangler config の存在を前提に
// するためこの時点から置く。routes / KV binding / secrets は Issue #1 以降で追加。
{
  "name": "gmail-mcp",
  "main": "src/index.ts",
  "compatibility_date": "2026-07-16"
}
